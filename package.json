{
  "name": "vue-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node_modules/.bin/vite",
    "build": "http-server /app/ && node_modules/.bin/vite build",
    "preview": "node_modules/.bin/vite preview",
    "watch": "(http-server /app/ && node_modules/.bin/vite build --watch)"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.0.0"
  }
}
